3U问题
* 在外参考模式下 切换界面 外参考会闪烁，重新刷了一次，所以要更改一下 在设置默认界面的时候检测一下外参考是否有打开 --未修复，应该是切换界面重绘导致的 再测试一下
* 开机的时候聚焦会从功率跳到频率，如果设置到功率，就会从频率跳到功率？？ 另外一个前面板没有这个问题（不连接PS）
* 旋钮按键的enter不能使用，我给屏蔽了应该是  --√
* 屏幕闪的问题 -- 以前的代码不闪 鼓捣一个最小的驱动不带任何功能的，看看闪不闪
    不连接power，单驱动不闪
    Hardware里的驱动全部粘贴过来，还是闪烁
    lvgl_conf粘贴过来 还是闪
* 文本框的输入长度设置 --√
* 光标位置 移动到最后一位 --√